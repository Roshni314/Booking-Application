<h1>Category List</h1>
<table class="index">
    <thead>
        <th>Name</th> 
    </thead>
    <tbody>
    <% @categories.each do |f| %>
        <tr>
            <td><%= f.name %></td>
        </tr>
        <% end %>
    </tbody>
</table>
 <%= link_to "New Category", new_category_path %> 

